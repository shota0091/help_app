.SearchForm
  = search_form_for @q  do |f|
    = f.search_field  :title_or_explanation_or_tags_tag_name_cont, placeholder: "検索内容を入力"
    = f.select :address_id,options_from_collection_for_select(Address.all, :id, :name),{prompt:"登録地を選択"}
    = f.submit '検索'
    並び替え
    = sort_link(@q, :created_at, "新着順")
    = sort_link(@q, :solution, "受付中")